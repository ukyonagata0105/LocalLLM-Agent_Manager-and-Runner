# M07: PDF Reader 詳細設計書

> **モジュールID**: M07  
> **Tier**: 3 (Advanced - 明示的に有効化)  
> **依存**: M05

---

## 1. 概要

研究用PDF閲覧。見開き表示、左右開き対応、注釈、RAG連携。

---

## 2. 制限事項（gemini-request.md反映）

| 項目 | 制限 | 理由 |
|------|------|------|
| **PDF解析** | pdf.jsによる簡易抽出（複雑レイアウトは未対応） | 実装難易度 |
| **高精度モード** | クラウドOCRへのフォールバック（オプション） | ローカル限界 |
| **注釈保存** | 独自DB形式（他リーダーと非互換） | 実装簡易化 |

---

## 3. 表示モード

### 3.1 見開き表示

| モード | 説明 |
|--------|------|
| 単一ページ | 1ページずつ |
| 見開き | 2ページ並列 |
| 連続スクロール | 縦スクロール |

### 3.2 開き方向

```yaml
display:
  spread_mode: left  # left（縦書き）、right（横書き）
  first_page: cover  # cover（表紙単独）、spread（見開き開始）
```

---

## 4. 注釈機能

```typescript
interface Annotation {
  id: string;
  type: 'highlight' | 'underline' | 'note' | 'drawing';
  page: number;
  position: BoundingBox;
  content?: string;
  color: string;
  createdAt: Date;
}
```

---

## 5. RAG連携

### 5.1 テキスト選択→RAG

```typescript
interface PDFtoRAG {
  // 選択テキストをRAGに登録
  addToKnowledgeBase(
    selection: TextSelection,
    metadata: { title: string; page: number }
  ): Promise<void>;
  
  // 選択テキストについてLLMに質問
  askAboutSelection(
    selection: TextSelection,
    question: string
  ): Promise<string>;
}
```

---

## 6. UI

```
┌─────────────────────────────────────────────────────────────┐
│ Machine_Learning.pdf                    [🔍] [📑] [💾]      │
├─────────────────────────────────────────────────────────────┤
│ ┌───────────────────────┬───────────────────────┐          │
│ │                       │                       │          │
│ │                       │                       │          │
│ │      Page 24          │       Page 25         │          │
│ │                       │                       │          │
│ │    [選択テキスト]      │                       │          │
│ │                       │                       │          │
│ └───────────────────────┴───────────────────────┘          │
├─────────────────────────────────────────────────────────────┤
│ 選択: "深層学習の基礎..."                                   │
│ [RAGに追加] [LLMに質問] [ハイライト]                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. モジュールAPI

```typescript
export interface PDFReaderAPI {
  // ファイル
  open(path: string): Promise<PDFDocument>;
  close(docId: string): void;
  
  // 注釈
  addAnnotation(docId: string, annotation: Annotation): void;
  getAnnotations(docId: string): Annotation[];
  
  // RAG連携
  extractText(docId: string, pages?: number[]): Promise<string>;
  addToRAG(docId: string, selection: TextSelection): Promise<void>;
}
```

---

## 8. 技術スタック

| 技術 | 用途 | ライセンス |
|------|------|------------|
| **pdf.js** | PDF描画 | Apache 2.0 |
| **pdf-lib** | PDF操作 | MIT |

---

*バージョン: 2.0*
*最終更新: 2026-01-05*
