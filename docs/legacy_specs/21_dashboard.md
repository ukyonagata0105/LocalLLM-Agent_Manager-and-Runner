# M21: Dashboard 詳細設計書

> **モジュールID**: M21  
> **Tier**: 1 (Core - 常に有効)  
> **依存**: M01, M10, M18  
> **被依存**: なし

---

## 1. 概要

システム全体を統括する中央指令室（コックピット）。
全モジュールのステータスを一元管理し、LLMとのチャットを通じてシステム開発やタスク実行を行うための統合インターフェースを提供します。

---

## 2. 制限事項（gemini-request.md反映）

| 項目 | 制限 | 理由 |
|------|------|------|
| **データ更新** | ポーリング（3秒〜10秒）またはSocket.io | リアルタイム性と負荷のバランス |
| **表示レイアウト** | グリッドシステムによるウィジェット配置 | ユーザーカスタマイズ性 |
| **開発機能** | ファイルツリーと簡易エディタを内蔵 | 外部IDEとの共存 |

---

## 3. データ集約アーキテクチャ (OpenHands Integration)

ダッシュボードは **OpenHands の UI を拡張** する形で実装されます。
iframe または Web Component として OpenHands の標準画面（チャット、ターミナル）を取り込み、その周囲に本システム独自の管理ウィジェットを配置します。

### 3.1 データソースマップ

| ウィジェット | データソース | 取得方法 |
|-------------|--------------|----------|
| **Agent Interface** | OpenHands | React Component / Iframe |
| **Terminal** | OpenHands | WebSocket Stream |
| **File Explorer** | OpenHands | Sandbox API |
| **Active Tasks** | M09 (Manager) | Local SQLite |
| **Long-term Mem** | M14 (Manager) | Vector DB |
| **Cost / Usage** | M18 (Manager) | Local SQLite |

---

## 4. 統合チャットインターフェース (Extended IDE)

OpenHands の標準チャットに対し、**コンテキスト注入** 機能を追加します。

### 4.1 機能拡張
- **Memory Injection**: M14から取得した過去の関連タスク情報を、プロンプトの `System Message` または `Context` に自動追加。
- **Task Awareness**: 現在の M09 タスクのステータスと要件を常にプロンプトに含め、OpenHandsのエージェントが「今何をすべきか」を見失わないようにする。
- **Supervisor Intervention**: ユーザーが「承認」「修正」ボタンを押すまで OpenHands の次のアクションをブロックする Human-in-the-Loop 制御。

---

## 5. UI設計 (Hybrid Layout)

```
┌─────────────────────────────────────────────────────────────┐
│ [Dashboard] [Tasks] [Memory] [Settings]             [Run ▶] │
├──────────────┬────────────────────────────────┬─────────────┤
│ **M09 Task** │ **OpenHands Interface**        │ **M14 Mem** │
│              │                                │             │
│ [TODO]       │ ┌──────────────────────────┐   │ [Context]   │
│ - Fix bug    │ │ 🤖 Agent:                │   │ > Similar   │
│ - Add test   │ │  I will run tests now.   │   │   task #12  │
│              │ └──────────────────────────┘   │             │
│              │                                │             │
│              │ ┌──────────────────────────┐   │             │
│              │ │ $ npm test               │   │             │
│              │ │ PASS                     │   │             │
│              │ └──────────────────────────┘   │             │
│              │                                │             │
│              │ [ Chat Input...          ]     │             │
│              │                                │             │
└──────────────┴────────────────────────────────┴─────────────┘
```

### 5.1 コンポーネント構成
- **Center Pane**: OpenHands のメインインターフェース（チャット & ワークスペース）。
- **Left Pane**: M09 タスク管理（カンバン/リスト）。ドラッグ＆ドロップでタスクをチャットに投入可能。
- **Right Pane**: M14 記憶 & M05 資料検索。エージェントに与えるコンテキストを可視化・編集。

---

## 6. モジュールAPI

```typescript
export interface DashboardAPI {
  // ウィジェット管理
  registerWidget(widget: WidgetDefinition): void;
  updateWidgetData(id: string, data: any): void;
  
  // 通知
  pushNotification(notification: Notification): void;
  
  // レイアウト
  saveLayout(layout: LayoutConfig): Promise<void>;
  loadLayout(): Promise<LayoutConfig>;
}
```

---

## 7. 技術スタック

| 技術 | 用途 | ライセンス |
|------|------|------------|
| **React Grid Layout** | ドラッグ＆ドロップ可能なグリッド | MIT |
| **XTerm.js** | Webベースのターミナルエミュレータ | MIT |
| **Monaco Editor** | コードエディタコンポーネント | MIT |
| **Socket.io-client** | リアルタイム通信 | MIT |

---

*バージョン: 2.0*
*最終更新: 2026-01-05*
