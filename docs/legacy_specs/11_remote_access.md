# M11: Remote Access 詳細設計書

> **モジュールID**: M11  
> **Tier**: 4 (Server - サーバーモード専用)  
> **依存**: M10  
> **追加要件**: Cloudflare Tunnel / Tailscale

---

## 1. 概要

外出先からのアクセス。PWA、セキュアトンネル、2要素認証。

---

## 2. 制限事項（gemini-request.md反映）

| 項目 | 制限 | 理由 |
|------|------|------|
| **トンネル依存** | 代替として直接IP表示オプション | サービス障害対策 |
| **モバイルUI** | 閲覧・承認のみ（編集機能なし） | UX複雑性回避 |
| **同時接続** | 最大5デバイス | リソース制限 |

---

## 3. トンネル設定

### 3.1 対応サービス

| サービス | 推奨 | 備考 |
|----------|------|------|
| **Cloudflare Tunnel** | ✅ | 無料、ゼロトラスト |
| **Tailscale** | ✅ | P2P VPN |
| **直接接続** | △ | VPN併用推奨 |

### 3.2 フォールバック

> ⚠️ **改善**: トンネル接続失敗時の代替

```typescript
interface ConnectionFallback {
  async connect(): Promise<Connection> {
    try {
      return await cloudflare.connect();
    } catch {
      // フォールバック: ローカルIP表示
      const localIP = await getLocalIP();
      ui.showNotification(
        `トンネル接続失敗。VPN経由で ${localIP}:3000 に接続してください`
      );
      return localServer;
    }
  }
}
```

---

## 4. モバイルUI

> ⚠️ **改善**: 機能を絞った専用UI

### 4.1 対応機能

| 機能 | モバイル対応 |
|------|-------------|
| ダッシュボード閲覧 | ✅ |
| ワークフロー実行 | ✅ |
| タスク確認・承認 | ✅ |
| ワークフロー編集 | ❌ (Read Only) |
| 設定変更 | ❌ |

### 4.2 UI

```
┌─────────────────────────┐
│ LocalLLM Agent      ☰  │
├─────────────────────────┤
│                         │
│ ⏸️ 承認待ち (2)          │
│ ┌─────────────────────┐ │
│ │ ファイル書き込み    │ │
│ │ /path/to/file.txt   │ │
│ │ [拒否] [承認]       │ │
│ └─────────────────────┘ │
│                         │
│ 📊 実行中 (1)            │
│ ┌─────────────────────┐ │
│ │ 日報生成            │ │
│ │ ████████░░ 80%      │ │
│ └─────────────────────┘ │
│                         │
├─────────────────────────┤
│ [🏠] [📋] [💬] [⚙️]     │
└─────────────────────────┘
```

---

## 5. 認証

### 5.1 2要素認証

```typescript
interface AuthConfig {
  password: {
    hash: string;
    minLength: 12;
  };
  
  totp: {
    enabled: boolean;
    secret: string;
  };
  
  session: {
    maxAge: '24h';
  };
}
```

---

## 6. モジュールAPI

```typescript
export interface RemoteAccessAPI {
  // トンネル
  connect(): Promise<Connection>;
  disconnect(): Promise<void>;
  getConnectionStatus(): ConnectionStatus;
  
  // 認証
  authenticate(credentials: Credentials): Promise<Session>;
  validateSession(token: string): boolean;
}
```

---

## 7. 技術スタック

| 技術 | 用途 | ライセンス |
|------|------|------------|
| **Express** | Webサーバー | MIT |
| **Passport.js** | 認証 | MIT |
| **speakeasy** | TOTP | MIT |
| **Socket.io** | リアルタイム通信 | MIT |

---

*バージョン: 2.0*
*最終更新: 2026-01-05*
